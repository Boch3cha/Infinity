<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Infinity (clone de layout)</title>
  <style>
    :root{
      --bg:#0f1013;
      --panel:#17181d;
      --panel2:#1e2027;
      --text:#f4f6ff;
      --muted:#b5b9c7;
      --chip:#2a2d37;
      --stroke:rgba(255,255,255,.08);
      --accent:#3f46ff; /* topo roxo */
      --accent2:#ff2a6d;
      --radius:18px;
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background:var(--bg);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      overflow-x:hidden;
    }

    /* Top bar roxa (status/espacinho) */
    .topbar{
      height:44px;
      background:var(--accent);
    }

    /* Header */
    header{
      position:sticky;
      top:0;
      z-index:10;
      background:linear-gradient(to bottom, rgba(15,16,19,.95), rgba(15,16,19,.88));
      backdrop-filter: blur(10px);
      border-bottom:1px solid var(--stroke);
    }
    .header-inner{
      height:58px;
      display:flex;
      align-items:center;
      gap:12px;
      padding:0 14px;
      max-width:520px;
      margin:0 auto;
    }

    .icon-btn{
      width:40px;height:40px;
      border-radius:12px;
      border:1px solid var(--stroke);
      background:rgba(255,255,255,.02);
      display:grid;
      place-items:center;
      cursor:pointer;
    }
    .hamburger{
      width:18px;height:12px; position:relative;
    }
    .hamburger:before,.hamburger:after,.hamburger span{
      content:"";
      position:absolute;left:0;right:0;
      height:2px;border-radius:2px;
      background:rgba(255,255,255,.85);
    }
    .hamburger:before{top:0}
    .hamburger span{top:5px; opacity:.9}
    .hamburger:after{bottom:0; opacity:.75}

    .brand{
      display:flex;
      align-items:center;
      gap:10px;
      font-weight:800;
      letter-spacing:.5px;
      font-size:22px;
      flex:1;
      justify-content:center;
      user-select:none;
    }
    .brand .badge{
      width:22px;height:22px;border-radius:8px;
      background:linear-gradient(135deg,var(--accent2),#ffb400);
      box-shadow:0 10px 25px rgba(255,42,109,.25);
    }

    .header-actions{
      display:flex;
      gap:10px;
    }
    .circle{
      border-radius:999px;
    }
    .dot{
      width:8px;height:8px;border-radius:999px;
      background:#ff4d4d;
      position:absolute;right:10px;top:10px;
    }

    /* Page container */
    .page{
      max-width:520px;
      margin:0 auto;
      padding:18px 14px 96px;
    }

    h2{
      margin:18px 0 12px;
      font-size:30px;
      letter-spacing:.2px;
    }

    /* horizontal list */
    .h-scroll{
      display:flex;
      gap:14px;
      overflow:auto;
      padding-bottom:6px;
      scroll-snap-type:x mandatory;
      -webkit-overflow-scrolling:touch;
    }
    .h-scroll::-webkit-scrollbar{height:6px}
    .h-scroll::-webkit-scrollbar-thumb{background:rgba(255,255,255,.12); border-radius:999px}

    /* cards */
    .card{
      scroll-snap-align:start;
      min-width:260px;
      background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
      border:1px solid var(--stroke);
      border-radius:var(--radius);
      overflow:hidden;
      position:relative;
      box-shadow:0 14px 40px rgba(0,0,0,.35);
    }
    .poster{
      height:190px;
      background:
        radial-gradient(1000px 300px at -10% 110%, rgba(63,70,255,.35), transparent 60%),
        radial-gradient(900px 320px at 110% -10%, rgba(255,42,109,.28), transparent 55%),
        linear-gradient(135deg, #2b2f3a, #14161b);
      position:relative;
    }
    .poster::after{
      content:"";
      position:absolute; inset:0;
      background:linear-gradient(to top, rgba(0,0,0,.65), transparent 55%);
    }
    .rank{
      position:absolute;
      left:14px; top:14px;
      font-size:44px;
      font-weight:900;
      color:rgba(255,255,255,.22);
      text-shadow:0 10px 20px rgba(0,0,0,.35);
      z-index:2;
    }

    .card-body{
      padding:14px 14px 16px;
      background:linear-gradient(to bottom, rgba(0,0,0,.55), rgba(0,0,0,.20));
    }
    .title{
      font-size:20px;
      font-weight:800;
      margin:0 0 10px;
      line-height:1.15;
    }

    .chips{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
    }
    .chip{
      font-size:13px;
      padding:7px 12px;
      border-radius:999px;
      background:rgba(255,255,255,.08);
      border:1px solid var(--stroke);
      color:rgba(255,255,255,.88);
      white-space:nowrap;
    }

    /* section header with "Ver tudo" */
    .section-row{
      display:flex;
      align-items:center;
      justify-content:space-between;
      margin-top:26px;
      margin-bottom:12px;
      gap:10px;
    }
    .section-row h3{
      margin:0;
      font-size:28px;
      font-weight:900;
    }
    .see-all{
      padding:10px 14px;
      background:rgba(255,255,255,.06);
      border:1px solid var(--stroke);
      color:rgba(255,255,255,.88);
      border-radius:999px;
      font-weight:700;
      font-size:14px;
      cursor:pointer;
    }

    /* category tiles (grid) */
    .grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:12px;
    }
    .tile{
      border-radius:16px;
      border:1px solid var(--stroke);
      background:
        radial-gradient(600px 120px at 10% 20%, rgba(63,70,255,.25), transparent 55%),
        radial-gradient(600px 120px at 90% 80%, rgba(255,42,109,.18), transparent 55%),
        linear-gradient(135deg, #222431, #121318);
      height:86px;
      display:flex;
      align-items:flex-end;
      padding:12px;
      font-weight:900;
      font-size:16px;
      color:rgba(255,255,255,.92);
      position:relative;
      overflow:hidden;
    }
    .tile span{
      position:relative;
      z-index:2;
    }
    .tile::before{
      content:"";
      position:absolute; inset:0;
      background:linear-gradient(to top, rgba(0,0,0,.45), transparent 65%);
    }

    /* big feature hero */
    .hero{
      border-radius:22px;
      border:1px solid var(--stroke);
      overflow:hidden;
      background:linear-gradient(135deg,#2a2d37,#121318);
      box-shadow:0 18px 55px rgba(0,0,0,.45);
    }
    .hero-top{
      height:260px;
      position:relative;
      background:
        radial-gradient(900px 340px at 10% 105%, rgba(63,70,255,.32), transparent 60%),
        radial-gradient(700px 260px at 110% -10%, rgba(255,42,109,.24), transparent 56%),
        linear-gradient(135deg,#2c3140,#121318);
    }
    .hero-top::after{
      content:"";
      position:absolute; inset:0;
      background:linear-gradient(to top, rgba(0,0,0,.75), transparent 55%);
    }
    .hero-content{
      position:absolute;
      left:16px; right:16px; bottom:14px;
      z-index:2;
    }
    .hero-kicker{
      font-size:12px;
      color:rgba(255,255,255,.78);
      letter-spacing:.9px;
      text-transform:uppercase;
      font-weight:800;
      margin-bottom:6px;
    }
    .hero-title{
      font-size:26px;
      font-weight:950;
      margin:0 0 12px;
      line-height:1.05;
    }
    .hero-actions{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
    }
    .btn{
      border:1px solid var(--stroke);
      background:rgba(255,255,255,.08);
      color:rgba(255,255,255,.92);
      font-weight:900;
      padding:12px 16px;
      border-radius:999px;
      cursor:pointer;
      display:inline-flex;
      align-items:center;
      gap:10px;
    }
    .btn.primary{
      background:rgba(255,255,255,.14);
    }
    .play{
      width:0;height:0;
      border-left:10px solid rgba(255,255,255,.92);
      border-top:6px solid transparent;
      border-bottom:6px solid transparent;
      margin-left:2px;
    }

    /* "assistir" bar inside cards like screenshot */
    .watchbar{
      margin-top:12px;
      border-radius:999px;
      border:1px solid var(--stroke);
      background:rgba(255,255,255,.06);
      padding:12px 14px;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      font-weight:950;
      letter-spacing:.6px;
    }
    .watchbar .play{
      border-left-color:rgba(255,255,255,.88);
    }

    /* floating scroll-top */
    .fab{
      position:fixed;
      right:16px;
      bottom:16px;
      width:46px;height:46px;
      border-radius:16px;
      background:rgba(0,0,0,.35);
      border:2px solid rgba(255,42,109,.9);
      box-shadow:0 18px 50px rgba(0,0,0,.5);
      display:grid;
      place-items:center;
      cursor:pointer;
      z-index:20;
    }
    .fab:before{
      content:"";
      width:10px;height:10px;
      border-right:3px solid rgba(255,255,255,.9);
      border-top:3px solid rgba(255,255,255,.9);
      transform:rotate(-45deg);
      margin-top:4px;
    }

    /* small helper text blocks */
    .muted{
      color:var(--muted);
      font-size:14px;
      line-height:1.45;
      margin:10px 0 0;
    }

    /* desktop: center content & keep mobile feel */
    @media (min-width: 900px){
      body{display:flex;justify-content:center}
      .shell{width:430px}
    }
  </style>
</head>
<body>
  <div class="shell" style="width:100%">
    <div class="topbar" aria-hidden="true"></div>

    <header>
      <div class="header-inner">
        <button class="icon-btn" aria-label="Abrir menu">
          <div class="hamburger"><span></span></div>
        </button>

        <div class="brand" aria-label="PK Filmes">
          <span>PK FILMES</span>
          <span class="badge" aria-hidden="true"></span>
        </div>

        <div class="header-actions">
          <button class="icon-btn" aria-label="Favoritos">
            <!-- ícone simples -->
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path d="M12 17.3l-6.18 3.7 1.64-7.03L2 9.24l7.19-.62L12 2l2.81 6.62 7.19.62-5.46 4.73L18.18 21 12 17.3z"
                    stroke="rgba(255,255,255,.9)" stroke-width="1.6" stroke-linejoin="round"/>
            </svg>
          </button>
          <button class="icon-btn circle" aria-label="Perfil">
            <span style="width:18px;height:18px;border-radius:999px;border:2px solid rgba(255,255,255,.9);display:inline-block"></span>
          </button>
        </div>
      </div>
    </header>

    <main class="page">
      <h2>Top 10 da Semana</h2>

      <div class="h-scroll" role="list">
        <article class="card" role="listitem">
          <div class="poster">
            <div class="rank">1</div>
          </div>
          <div class="card-body">
            <p class="title">Título em Destaque</p>
            <div class="chips">
              <span class="chip">Aventura</span>
              <span class="chip">Ficção</span>
            </div>
          </div>
        </article>

        <article class="card" role="listitem" style="min-width:220px; opacity:.9">
          <div class="poster" style="height:190px;background:
            radial-gradient(900px 300px at 20% 110%, rgba(255,42,109,.28), transparent 60%),
            radial-gradient(900px 300px at 110% 0%, rgba(63,70,255,.22), transparent 55%),
            linear-gradient(135deg,#2a2d37,#121318);">
            <div class="rank">2</div>
          </div>
          <div class="card-body">
            <p class="title">Outro Título</p>
            <div class="chips">
              <span class="chip">Romance</span>
              <span class="chip">Drama</span>
            </div>
          </div>
        </article>
      </div>

      <div class="section-row">
        <h3>Categorias</h3>
      </div>
      <div class="grid">
        <div class="tile"><span>Doramas</span></div>
        <div class="tile"><span>Séries</span></div>
        <div class="tile"><span>Animes</span></div>
        <div class="tile"><span>Ação</span></div>
      </div>

      <div class="section-row">
        <h3>Disney 100</h3>
        <button class="see-all">Ver tudo</button>
      </div>

      <div class="h-scroll" role="list">
        <article class="card" role="listitem" style="min-width:320px">
          <div class="poster" style="height:160px;background:
            radial-gradient(900px 220px at 15% 110%, rgba(255,42,109,.22), transparent 60%),
            radial-gradient(900px 260px at 105% 0%, rgba(63,70,255,.26), transparent 60%),
            linear-gradient(135deg,#2a2d37,#121318);">
          </div>
          <div class="card-body">
            <p clss="title">Clássico / Coleção</p>
            <div class="chips">
              <span class="chip">Ação</span>
              <span class="chip">Comédia</span>
              <span class="chip">Disney</span>
            </div>
            <div class="watchbar"><span class="play" aria-hidden="true"></span> ASSISTIR</div>
          </div>
        </article>

        <article class="card" role="listitem" style="min-width:320px">
          <div class="poster" style="height:160px;background:
            radial-gradient(900px 220px at 10% 110%, rgba(63,70,255,.25), transparent 60%),
            radial-gradient(900px 260px at 110% 0%, rgba(255,180,0,.18), transparent 60%),
            linear-gradient(135deg,#2a2d37,#121318);">
          </div>
          <div class="card-body">
            <p class="title">Outro Destaque</p>
            <div class="chips">
              <span class="chip">Animação</span>
              <span class="chip">Aventura</span>
            </div>
            <div class="watchbar"><span class="play" aria-hidden="true"></span> ASSISTIR</div>
          </div>
        </article>
      </div>

      <div class="section-row">
        <h3>Agora no Catálogo</h3>
      </div>

      <section class="hero" aria-label="Destaque do catálogo">
        <div class="hero-top">
          <div class="hero-content">
            <div class="hero-kicker">AGORA NO CATÁLOGO</div>
            <h4 class="hero-title">Destaque Principal do Dia</h4>
            <div class="hero-actions">
              <button class="btn primary"><span class="play" aria-hidden="true"></span> PLAY</button>
              <button class="btn">DETALHES</button>
            </div>
          </div>
        </div>
      </section>

      <div class="section-row">
        <h3>Últimas Postagens</h3>
        <button class="see-all">Ver tudo</button>
      </div>

      <div class="h-scroll" role="list">
        <article class="card" role="listitem" style="min-width:360px">
          <div class="poster" style="height:150px;background:
            radial-gradient(900px 240px at 12% 110%, rgba(255,42,109,.25), transparent 62%),
            radial-gradient(900px 240px at 110% 0%, rgba(63,70,255,.22), transparent 60%),
            linear-gradient(135deg,#2a2d37,#121318);">
          </div>
          <div class="card-body">
            <p class="title">Postagem Recente</p>
            <div class="chips">
              <span class="chip">Aventura</span>
              <span class="chip">Comédia</span>
              <span class="chip">Terror</span>
            </div>
            <div class="watchbar"><span class="play" aria-hidden="true"></span> ASSISTIR</div>
          </div>
        </article>

        <article class="card" role="listitem" style="min-width:260px">
          <div class="poster" style="height:150px;background:
            radial-gradient(900px 240px at 20% 110%, rgba(63,70,255,.28), transparent 62%),
            radial-gradient(900px 240px at 110% 0%, rgba(255,42,109,.16), transparent 60%),
            linear-gradient(135deg,#2a2d37,#121318);">
          </div>
          <div class="card-body">
            <p class="title">Outro Post</p>
            <div class="chips">
              <span class="chip">Lançamento</span>
            </div>
            <div class="watchbar"><span class="play" aria-hidden="true"></span> ASSISTIR</div>
          </div>
        </article>
      </div>

      <p class="muted">
        Observação: este arquivo é um <b>clone de layout</b> (estrutura, cores e estilo mobile).
        As “capas” são placeholders (gradientes), sem uso de imagens protegidas.
      </p>
    </main>

    <button class="fab" aria-label="Voltar ao topo" onclick="window.scrollTo({top:0, behavior:'smooth'})"></button>
  </div>
</body>
</html>
